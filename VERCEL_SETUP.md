# إعداد المشروع للرفع على Vercel

## ✅ الإصلاحات المنفذة

### 1. إصلاح أخطاء TypeScript
- ✅ حذف التعريف المكرر لـ `User` interface
- ✅ توحيد تعريف `User` في `types.ts`
- ✅ إصلاح type conversions في `fetchServices`

### 2. إعدادات Vercel
- ✅ إنشاء `vercel.json` في الجذر
- ✅ تحديث `next.config.mjs` للبناء
- ✅ إزالة `output: 'standalone'` (يسبب مشاكل مع Vercel)

### 3. الملفات المحدثة
- `client/lib/api-client.ts` - إصلاح تعارض `User` interface
- `client/lib/types.ts` - توحيد تعريف `User`
- `client/next.config.mjs` - إعدادات البناء
- `vercel.json` - إعدادات Vercel

## 📋 خطوات الرفع على Vercel

### 1. إعداد المشروع محلياً

```bash
# الانتقال لمجلد client
cd client

# تثبيت الحزم
npm install

# اختبار البناء (إذا كان يعمل)
npm run build
```

### 2. رفع الكود إلى GitHub

```bash
git add .
git commit -m "Prepare for Vercel deployment"
git push origin main
```

### 3. ربط المشروع بـ Vercel

1. اذهب إلى [vercel.com](https://vercel.com)
2. اضغط "Add New Project"
3. اختر repository الخاص بك
4. في إعدادات المشروع:
   - **Framework Preset**: Next.js (سيتم اكتشافه تلقائياً)
   - **Root Directory**: `client` (مهم جداً!)
   - **Build Command**: `npm run build` (افتراضي)
   - **Output Directory**: `.next` (افتراضي)

### 4. إعداد متغيرات البيئة

في Vercel Dashboard → Settings → Environment Variables:

```
NEXT_PUBLIC_API_BASE_URL=https://your-backend-api.com/api
```

**ملاحظة مهمة**: استبدل `https://your-backend-api.com/api` بعنوان API الخاص بك.

### 5. الرفع

بعد إعداد كل شيء، اضغط "Deploy". Vercel سيقوم بـ:
1. تثبيت الحزم
2. بناء المشروع
3. رفعه على الإنترنت

## ⚠️ ملاحظات مهمة

### مشكلة البناء المحلي

إذا كان `npm run build` لا يعمل محلياً بسبب خطأ webpack، هذا طبيعي. Vercel يستخدم بيئة مختلفة وقد يعمل البناء هناك.

### إذا فشل البناء على Vercel

1. تحقق من Build Logs في Vercel
2. تأكد من أن `Root Directory` مضبوط على `client`
3. تأكد من وجود متغيرات البيئة
4. تحقق من أن جميع الحزم مثبتة في `package.json`

### CORS في الباك اند

تأكد من إعداد CORS في Django للسماح بـ Vercel domain:

```python
# settings.py
CORS_ALLOWED_ORIGINS = [
    "https://your-project.vercel.app",
    # أو
    "https://*.vercel.app",  # لجميع مشاريع Vercel
]
```

## 📝 الملفات المهمة

- `vercel.json` - إعدادات Vercel (في الجذر)
- `client/next.config.mjs` - إعدادات Next.js
- `client/package.json` - الحزم والـ scripts
- `client/.env.production` - متغيرات البيئة (لا ترفعها للـ git!)

## ✅ الحالة الحالية

- ✅ تم إصلاح جميع أخطاء TypeScript
- ✅ تم إعداد `vercel.json`
- ✅ تم تحديث `next.config.mjs`
- ⚠️ قد يكون هناك خطأ webpack محلي (لكن Vercel قد يعمل)

## 🚀 الخطوة التالية

1. ارفع الكود إلى GitHub
2. اربط المشروع بـ Vercel
3. أضف متغيرات البيئة
4. اضغط Deploy!

---

**ملاحظة**: إذا استمرت المشاكل، تحقق من Build Logs في Vercel للحصول على تفاصيل أكثر.

