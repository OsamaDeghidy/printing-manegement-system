# إعداد RTL/LTR لدعم العربية والإنجليزية في Django Admin

## ✅ ما تم إضافته

1. **ملف CSS مخصص** (`project/static/admin/css/jazzmin_rtl.css`)
   - دعم كامل لـ RTL (العربية)
   - دعم كامل لـ LTR (الإنجليزية)
   - السايد بار على اليمين للعربية وعلى اليسار للإنجليزية

2. **ملف JavaScript** (`project/static/admin/js/jazzmin_rtl.js`)
   - كشف تلقائي للغة من Django
   - تطبيق RTL/LTR تلقائياً

3. **إعدادات Django**
   - تفعيل `LocaleMiddleware`
   - إضافة دعم للغتين (العربية والإنجليزية)
   - تفعيل `language_chooser` في Jazzmin

## 📦 التثبيت

### 1. جمع الملفات الثابتة

```bash
cd server
python manage.py collectstatic --noinput
```

### 2. إنشاء ملفات الترجمة (اختياري)

```bash
# إنشاء ملفات الترجمة
python manage.py makemessages -l ar
python manage.py makemessages -l en

# ترجمة الملفات
python manage.py compilemessages
```

### 3. تشغيل السيرفر

```bash
python manage.py runserver
```

## 🌐 استخدام اختيار اللغة

### في Django Admin:

1. افتح `/admin/`
2. ستجد قائمة اختيار اللغة في أعلى الصفحة (إذا كان `language_chooser: True`)
3. اختر اللغة المطلوبة

### برمجياً:

```python
# في views.py
from django.utils import translation

# تغيير اللغة للعربية
translation.activate('ar')

# تغيير اللغة للإنجليزية
translation.activate('en')
```

### في Templates:

```django
{% load i18n %}

{% get_current_language as LANGUAGE_CODE %}
{% if LANGUAGE_CODE == 'ar' %}
    <p>اللغة العربية</p>
{% else %}
    <p>English</p>
{% endif %}
```

## 🎨 التخصيص

### تغيير اللغة الافتراضية:

في `settings.py`:
```python
LANGUAGE_CODE = "ar"  # أو "en"
```

### إضافة لغات أخرى:

```python
LANGUAGES = [
    ('ar', 'العربية'),
    ('en', 'English'),
    ('fr', 'Français'),  # مثال
]
```

### تخصيص CSS:

يمكنك تعديل `project/static/admin/css/jazzmin_rtl.css` حسب احتياجاتك.

## 🔍 التحقق من العمل

1. افتح `/admin/` - يجب أن ترى السايد بار على اليمين (RTL)
2. غير اللغة إلى الإنجليزية - يجب أن ينتقل السايد بار إلى اليسار (LTR)
3. تحقق من أن النصوص والجداول تتبع اتجاه اللغة المختارة

## 📝 ملاحظات

- السايد بار ينتقل تلقائياً بناءً على اللغة المختارة
- جميع العناصر (النماذج، الجداول، الأزرار) تتبع اتجاه اللغة
- الواجهة متجاوبة وتعمل على جميع الأجهزة

## 🐛 حل المشاكل

### إذا لم يظهر السايد بار على اليمين:

1. تأكد من جمع الملفات الثابتة: `python manage.py collectstatic`
2. تأكد من أن `STATICFILES_DIRS` يشمل `project/static`
3. امسح cache المتصفح (Ctrl+F5)

### إذا لم يعمل اختيار اللغة:

1. تأكد من تفعيل `LocaleMiddleware` في `MIDDLEWARE`
2. تأكد من إضافة `path("i18n/", include("django.conf.urls.i18n"))` في `urls.py`
3. تأكد من أن `language_chooser: True` في `JAZZMIN_SETTINGS`

---

**ملاحظة:** بعد التثبيت، افتح `/admin/` وستجد الواجهة تدعم RTL تلقائياً للعربية وLTR للإنجليزية!

